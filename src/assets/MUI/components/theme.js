import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#026b77",
      light: "#348892",
      dark: "#014A53",
    },
    secondary: {
      main: "#ffc400",
      light: "#FFCF33",
      dark: "#B28900",
    },
  },
});
